def ffn(x, d_ff=256):
    d = x.shape[-1]

    W1 = np.random.randn(d, d_ff)
    b1 = np.zeros(d_ff)
    W2 = np.random.randn(d_ff, d)
    b2 = np.zeros(d)

    h = np.maximum(0, x @ W1 + b1) 
    return h @ W2 + b2
